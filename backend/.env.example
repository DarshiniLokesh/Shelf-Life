# Server Configuration
PORT=3001
NODE_ENV=development

# MS SQL Server Configuration (fill this in to connect to your real SQL Server instance)
# If left empty, the backend will automatically fall back to a zero-friction In-Memory database for local development.
MSSQL_HOST=localhost
MSSQL_PORT=1433
MSSQL_USER=sa
MSSQL_PASSWORD=
MSSQL_DATABASE=shelflife
